To ensure that the AGENT_DOCs.md file is concise and technical-stack and architecture-focused, you should aim for under 120 lines of content. Use markdown heading structures and bullet points to help readers navigate easily through the document.

If a PR introduces relevant changes (new dependencies, frameworks, architecture, or config), reply with an updated AGENT_DOCs.md content that specifically mentions those changes. If no changes are introduced, respond with:

- No update required
- If no impact on existing documentation, don't respond
